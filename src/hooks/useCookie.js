export const useCookie = () => {
  const saveAuthCookie = (key) => {};

  const deleteAuthCookie = (key) => {};

  const getAuthCookie = (key) => {};

  const isAuthCookieExpired = (key) => {};

  const hasValidAuthCookie = (key) => {};

  return {
    saveAuthCookie,
    deleteAuthCookie,
    getAuthCookie,
    isAuthCookieExpired,
    hasValidAuthCookie,
  };
};
